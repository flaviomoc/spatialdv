#' SpatRaster and data.frame objects
#'
#' *ref.tif* is an object of class SpatRaster created from a random array representing binary maps of 10 species in the reference scenario.
#' *fut.tif* is an object of class SpatRaster created from a random array representing binary maps of 10 species in the future scenario.
#' *traits.xlsx* is an object of class data.frame created from random vectors representing species traits: body mass and elevation.
#' @format SpatRaster: 10 columns, 10 rows, and 10 layers. Data.frame: 2 columns and 10 rows.
#' @source Random values
"data"
